# Kryos SDK

The Kryos SDK allows companies/startups to securely log their data & transaction events into Kryos,
where each action is hashed and stored on the blockchain for immutable auditing.

## Installation
Install directly from GitHub:

```bash
pip install git+https://github.com/Shubh1127/kryos-SDK.git
